可以直接用这一句：

**请用 Python 交互式（REPL）教学方式讲解这段代码：输出一套可以直接复制执行的命令，每次只执行一行代码，每行都加一句注释说明作用，并在关键步骤让我查看变量值、shape 和中间结果，使用尽可能简单的示例参数。**

如果想进一步强调学习体验，可以用下面这个版本（推荐）：

**请把这段代码改造成 Python REPL 教程：按执行顺序输出可直接复制的一行一行命令（不要封装成函数），每行附一句注释，使用最简单的示例参数，并在每个关键步骤打印变量值、shape 和中间结果，让我能逐步理解整个执行过程。**

第二个版本适合绝大多数深度学习代码（PyTorch、Transformer、LLaMA 等），我建议以后固定使用这个提示词。





更推荐这个提示词：

**请把这段代码改造成 Jupyter Notebook 教程：按执行顺序拆成多个代码单元，每个单元只做一件事；使用最简单的示例参数；每个关键单元都打印变量值、shape 和中间结果；每个代码单元前用简短 Markdown 说明它在做什么，不要封装成复杂函数。**

更精简版：

**请把这段代码改造成 Jupyter Notebook 风格的逐步教程：小代码单元 + 简短 Markdown 说明 + 简单示例参数 + 关键变量值、shape、中间结果打印。**