可以总结成下面这份学习优先级：

## **必须深入学习**

这是 **LLM 核心原理**：

- Transformer（Transformer 架构）
- Attention（注意力机制）
- RoPE（Rotary Position Embedding，旋转位置编码）
- RMSNorm（均方根归一化）
- SwiGLU（门控前馈网络）
- GQA / MQA（分组查询 / 多查询注意力）
- MoE（Mixture of Experts，混合专家模型）
- Next Token Prediction（下一个 Token 预测）
- Cross Entropy（交叉熵损失）
- Backpropagation（反向传播）
- AdamW（优化器）
- Training vs Inference（训练与推理的区别）
- KV Cache（KV 缓存）

**目标：能从原理和代码层面说清楚。**

## **理解原理即可**

这是 **训练策略和单机工程增强**：

- Warmup（学习率预热）
- Cosine Decay（余弦学习率衰减）
- Gradient Clipping（梯度裁剪）
- Gradient Accumulation（梯度累积）
- Mixed Precision（混合精度训练）
- `torch.compile`（模型编译优化）

**目标：知道是什么、解决什么问题、代码大概怎么实现。不需要逐行研究。**

## **当前直接跳过**

这是 **大规模分布式训练工程**：

- DDP（Distributed Data Parallel，分布式数据并行）
- NCCL（英伟达 GPU 通信库）
- FSDP（Fully Sharded Data Parallel，全分片数据并行）
- ZeRO（零冗余优化）
- Tensor Parallel（张量并行）
- Pipeline Parallel（流水线并行）
- InfiniBand（高速集群网络）
- 多机多卡通信、网络拓扑

**目标：当前不学。以后面试需要时单独补“大模型分布式训练”专题。**

## **你的当前路线**

```text
AdamW
→ 简单看 Warmup + Cosine Decay
→ 知道梯度裁剪、梯度累积、混合精度的作用
→ DDP 跳过
→ 结束 LLaMA2.c
→ 快速对照 Meta 官方 LLaMA
→ 进入 Qwen
```

一句话总结：

**深入学模型结构和训练/推理本质；训练增强机制理解原理；大规模分布式训练工程暂时跳过。**